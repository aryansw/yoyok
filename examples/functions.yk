fn run(x: i32) -> i32 {
  if x > 0 {
    0
  } else {
    5 + 'a'
  }
}

fn main() -> i32 {
  # Yes, higher order functions are supported.
  let q: (i32) -> i32 = run;
  let x = q(-12);
  var y: i32 = if x > 0 {
      0
  } else {
      1
  };
  while y < x {
      y = y + 1;
  };
  x - 12
}